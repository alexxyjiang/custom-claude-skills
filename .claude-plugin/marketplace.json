{
  "name": "custom-claude-skills",
  "owner": {
    "name": "alexxyjiang (Alex Jiang)",
    "email": "alexxyjiang@gmail.com"
  },
  "plugins": [
    {
      "name": "costar-coach-plugin",
      "source": "./plugins/costar-coach-plugin",
      "description": "Converts rough descriptions or ideas into well-structured prompts using the COSTAR framework (Context, Objective, Style, Tone, Audience, Response). Coaches patiently -- asks one focused question when a component is missing rather than guessing -- and embeds the same patient, ask-don't-guess behavior into every generated prompt. Offers to execute the prompt immediately after presenting it."
    },
    {
      "name": "crispe-coach-plugin",
      "source": "./plugins/crispe-coach-plugin",
      "description": "Converts rough descriptions or ideas into well-structured prompts using the CRISPE framework (Context, Role, Instruction, Specification, Performance, Example). Coaches patiently -- asks one focused question when a component is missing rather than guessing -- and embeds the same patient, ask-don't-guess behavior into every generated prompt. Offers to execute the prompt immediately after presenting it."
    },
    {
      "name": "auto-skill-update-plugin",
      "source": "./plugins/auto-skill-update-plugin",
      "description": "Automatically detects when a skill was recently used and the user signals it should be improved, then applies minimal, surgical edits to the skill definition -- covering new trigger patterns, handling cases, or examples without rewriting wholesale."
    },
    {
      "name": "prompt-convert-trigger-plugin",
      "source": "./plugins/prompt-convert-trigger-plugin",
      "description": "Automatically detects complex or multi-step tasks and converts them into structured COSTAR or CRISPE prompts. Triggers proactively when user requests involve 3+ steps, multiple conditions, vague requirements, multi-file scope, or research plus implementation work."
    },
    {
      "name": "crispe-system-architect-plugin",
      "source": "./plugins/crispe-system-architect-plugin",
      "description": "Expert program architecture analyzer that generates documentation for any code repository. Supports three output styles -- Choice 1: CONTRIBUTING.md + STRUCTURE.md (contribution guide alongside a separate structure reference), Choice 2: CLAUDE.md (single merged AI context file), Choice 3: STRUCTURE.md only (structure reference without a contribution guide). STRUCTURE.md, ARCHITECTURE.md, and OVERVIEW.md are treated as equivalent -- update the one that already exists, or create STRUCTURE.md if none is present. Trigger this skill proactively when the user requests a code change but no documentation file exists in the repo root. Also trigger explicitly when the user says \"analyze this repo\", \"generate STRUCTURE.md\", \"generate CLAUDE.md\", \"generate CONTRIBUTING.md\", \"summarize the codebase\", \"document the project structure\", \"what is the architecture of this project\", or any equivalent phrasing about understanding or mapping an unfamiliar codebase. When in doubt, trigger -- generating an architectural reference once saves every future contributor from re-discovering the same things."
    }
  ]
}
